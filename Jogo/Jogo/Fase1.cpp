#include "Fase1.h"
#include "stdafx.h"
/*
void Fase1::initTextura()
{
    if (!image.loadFromFile("Texturas/Ground.png"))
    {
        cout << "Load chao Failed " << endl;
        system("pause");
    }
    if (!tBackGround.loadFromFile("Texturas/BackGround.png"))
    {
        cout << "Load chao Failed " << endl;
        system("pause");
    }
    image.createMaskFromColor(sf::Color::White);
    textura.loadFromImage(image);
    textura.setRepeated(true);
}

void Fase1::initSprite()
{
    sBackGround.setTexture(tBackGround);
    sprite.setTexture(textura);
    sprite.setScale(1.f, 2.f);
}

Fase1::Fase1()
{
    initTextura();
    initSprite();
}

Fase1::~Fase1()
{
}

void Fase1::initScenery()
{
    window->draw(sBackGround);
    sprite.setPosition(sf::Vector2f(0.f, 550.f));
    sprite.setTextureRect(sf::IntRect(0, 0, 800, 29));
    window->draw(sprite);
    sprite.setPosition(sf::Vector2f(0.f, 400.f));
    sprite.setTextureRect(sf::IntRect(0, 0, 700, 29));
    window->draw(sprite);
    sprite.setPosition(sf::Vector2f(100.f, 250.f));
    sprite.setTextureRect(sf::IntRect(0, 0, 800, 29));
    window->draw(sprite);
    sprite.setPosition(sf::Vector2f(0.f, 100.f));
    sprite.setTextureRect(sf::IntRect(0, 0, 700, 29));
    window->draw(sprite);
}
*/

